# Server configuration
PORT=9808
HOST=0.0.0.0

# Redis configuration
REDIS_HOST=redis           # or 'localhost' if running Redis locally
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=            # leave empty if Redis has no password

# Application settings
APP_BASE_URL=http://localhost:9808

# Default behavior (used for testing/demo)
DEFAULT_USER=test_user
